syntax = "proto3";

option csharp_namespace = "CraftAI.Gate.Service";

import "greet.proto";
package craftai.handshake;
// The greeting service definition.
service Handshake {
  // Sends a greeting
  rpc SayHello (greet.HelloRequest) returns (greet.HelloReply);
}